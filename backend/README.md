# backend

FastAPI service scaffold for `fitness-app`.

## Run locally

```bash
python -m venv .venv
.venv\\Scripts\\activate
pip install -e .
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```
